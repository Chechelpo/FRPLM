{
  "name": "@frplm/ui",
  "version": "0.2.0",
  "private": false,
  "type": "module",
  "description": "Reusable Vue 3 primitive editor components for FRPLM. Built as a Vite library; exports the components as an ES module alongside a style.css that the host application loads once to keep extension panels visually consistent with the rest of the app. Maintained by AI agents.",
  "license": "MIT",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./style.css": "./dist/style.css"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build:js": "vite build --config vite.config.ts",
    "build": "npm run clean && npm run build:js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --experimental-strip-types --test tests/*.test.ts"
  },
  "peerDependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "typescript": "^5.9.3",
    "vite": "^7.3.6",
    "vue": "^3.5.25"
  }
}
